enum WordSort {
    ALPHA_ASC
    ALPHA_DESC
}

type Word {
    id: ID!
    english_word: String!
    categories: [Category!]! @belongsToMany
    translations: [Translation!]! @hasMany
    created_at: DateTime!
    updated_at: DateTime!
}